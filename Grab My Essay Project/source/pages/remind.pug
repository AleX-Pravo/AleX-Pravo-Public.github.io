doctype html
html(lang='ru')
    head
        include partials/_head.pug
        title Remind
        include partials/_styles.pug

    body
        = '\n'
        // layout
        .layout
            .layout__header
                include modules/_header.pug

            .layout__body
                .layout__content
                    main.layout__main

                        = '\n'
                        // login
                        .login
                            .login__title Remind password
                            .login__wrapper
                                .login__item.f_group
                                    .login__label E-mail
                                    .login__field.f_input
                                        input.login__input(type='text' placeholder='Enter your e-mail')

                                .login__action
                                    a.login__button.button.button--dark(href='#') Restore Password
                                .login__text Don’t have an account?
                                a.login__link(href='#') Place an order
                        // end login
                        = '\n'

            .layout__footer
                include modules/_footer.pug
                include modules/_promo.pug

        // end layout
        = '\n'
        include partials/_scripts.pug