//- Utility
include utils/_variable.pug

- pageTitle = 'pursuit-tag'
- nameTitle = 'Пошук за тегом'
- subTitle = ''

//-js
-
    //select2 = true
    //select2filters = true
    perfectScrollbar = true
    tab = true

//- selectric = true

doctype html
html
    include layout/_head.pug

    body

        = '\n'
        //= Layout =
        #layout.layout.tab
            include layout/_header.pug
            include layout/_header-device.pug

            //= Main =

            main#main.main.main--header
                .container
                    .breadcrumb--inner
                        include module/_breadcrumb.pug

                        h1.topBlock__title= nameTitle

                    include module/_bigboard.pug

                    .catalog#pursuitone.tab-box
                        //- (source/assets/css/module/_blog.scss)
                        .blog__wrapper
                            - for(var j = 0; j < 2; j++)
                                - for(var i = 0; i < 6; i++)
                                    - modifier = false
                                    include module/_blogs.pug

                        include module/_pagination.pug

                    .catalog#pursuittwo.tab-box
                        //- (source/assets/css/module/_blog.scss)
                        .blog__wrapper
                            - for(var j = 0; j < 2; j++)
                                - for(var i = 0; i < 6; i++)
                                    - modifier = false
                                    include module/_blogs.pug

                        include module/_pagination.pug

                    .catalog#pursuitthree.tab-box
                        //- (source/assets/css/module/_blog.scss)
                        .blog__wrapper
                            - for(var j = 0; j < 2; j++)
                                - for(var i = 0; i < 6; i++)
                                    - modifier = false
                                    include module/_blogs.pug

                        include module/_pagination.pug

                    .catalog#pursuitfour.tab-box
                        //- (source/assets/css/module/_blog.scss)
                        .blog__wrapper
                            - for(var j = 0; j < 2; j++)
                                - for(var i = 0; i < 6; i++)
                                    - modifier = false
                                    include module/_blogs.pug

                        include module/_pagination.pug
            //= End main =
            = '\n'

        //= End layout =

        include layout/_footer.pug
        //include module/_pageUp.pug
        include layout/_scripts.pug
